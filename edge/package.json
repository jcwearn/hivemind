{
  "name": "hivemind-edge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + Container wrapper that puts hivemind on the public internet.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260825.1",
    "typescript": "7.0.2",
    "wrangler": "4.125.0"
  }
}
